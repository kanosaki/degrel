
description: Rewrite simple vertex in one step.
- input: 
    cell: >
      {
        hoge(fuga: bar)
        hoge(fuga: _@X) -> piyo(foo: X)
      }

- assert:
    condition: stop
    cell: >
      {
        piyo(foo: bar)
        hoge(fuga: _@X) -> piyo(foo: X)
      }

# vim: ft=yaml sw=2 ts=2 sts=2
