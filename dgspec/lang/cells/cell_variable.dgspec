
caption: Cell variable.
description: >
  Cells can share vertices thru cell variables with lexical scope.
version: 1
seq_spec:
  - init: >
      {
        var variable = value
        (var(@Name) = @Value) -> Name(Value)
      }
  - rewrite
  - assert: >
      {
        variable(value) 
        (var(@Name) = @Value) -> Name(Value)
      }

# vim: ft=yaml sw=2 ts=2 sts=2
