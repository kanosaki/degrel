package degrel.core.operators

import degrel.core._

// Compare two graph by Structure and ID and Label
class GraphIsomorphismComparator(self: Graph, another: Graph) {

  def eval(): Boolean = {
    ???
  }
}
