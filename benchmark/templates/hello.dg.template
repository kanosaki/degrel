greet(@N, @Max) -> {
  {{ noise(index) }}
  original(@X) -> transoformed(plus_one(X + 1), plus_two(X + 2))

  fin if((N < Max), 
    then: {
      {{ noise(index) }}
      println("HELLO", N)
      fin greet((N + 1), Max)
    },
    else: {
      println("DONE", N)
    })
}

greet(0, 5000)
